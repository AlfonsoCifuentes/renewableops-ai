apiVersion: v1
kind: Secret
metadata:
  name: renewableops-secrets
  namespace: renewableops-app
type: Opaque
stringData:
  # Example only. Replace through SOPS, External Secrets, or a local secret manager.
  POSTGRES_PASSWORD: "replace-before-apply"
  WEBHOOK_HMAC_SECRET: "replace-before-apply"
