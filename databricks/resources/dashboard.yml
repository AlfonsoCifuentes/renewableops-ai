resources:
  dashboards:
    renewableops_operations:
      display_name: "RenewableOps AI · Operations"
      file_path: ../src/renewableops_operations.lvdash.json
      warehouse_id: ${var.warehouse_id}
      dataset_catalog: ${var.catalog}
      dataset_schema: ${var.schema}
      embed_credentials: false
